export const sum = (a,b) =>  {
    return a + b;
}; modulo
// se vuelve un modul cuando se quiere exportar para usarlo en otro archivo

export const multiply = (a,b) =>  {
    return a + b;
}; modulo
module.export = sum; 
// exportarlo como modulo


// module.export = {
//     sum,
//     multiply,
// }; cuando se qiieren expotar mas de un modulo


// export const multiply = (a,b) =>  {
//     return a + b;
// }; modulo
// para exportar esata es la mnera mas actualizada

export default sum 
// para exportar solo un modulo

// cuando se tiene mas de uno es mejor exportarlos con export delante de la funcion

// por ejemplo cuando se tiene que craer una funcon por ej por la fecha, se creria la funcion y se exportaria asi: 

// export default sum;